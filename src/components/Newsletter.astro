---
import Google from "../utils/google";
import Card from "./Card.astro";
import { QR } from "qr-svg";

const newsletter = QR(await Google.getNewsletter());
---

<Card title="Student Newsletter">
  <Fragment set:html={newsletter} />
</Card>
