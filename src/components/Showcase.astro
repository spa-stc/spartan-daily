---
import Google from "../utils/google";
import Card from "./Card.astro";
import RotatingImage from "./RotatingImage.astro";

const images = await Google.getImages();
---

<Card>
  <RotatingImage images={images} random={false} speed={30} clazz="max-h-md" />
</Card>
